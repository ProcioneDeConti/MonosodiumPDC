<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Scoped storage (which lets MediaDownloader insert into MediaStore without this permission)
         only applies on API 29+ devices; minSdk is 28 (Android 9), where the classic storage model
         still gates MediaStore.insert() on WRITE_EXTERNAL_STORAGE. maxSdkVersion so it's simply
         not requested/granted on the API 29+ devices most users are actually on. Still needs a
         runtime permission request added on the download path for it to take effect on API 23-28 -
         a bare manifest declaration alone doesn't grant a dangerous permission on those versions. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".E621Application"
        android:allowBackup="true"
        android:backupAgent=".data.backup.AppBackupAgent"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupOnly="true"
        android:fullBackupContent="@xml/backup_rules"
        android:hasFragileUserData="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.E621">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.E621"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- App Links for e621/e926/e6ai. autoVerify only takes effect if those sites ever
                 host a matching /.well-known/assetlinks.json (we don't control that third-party
                 server), so users may still need to enable this manually via Settings > Apps >
                 e621 > Open by default. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="e621.net" />
                <data android:host="www.e621.net" />
                <data android:host="e926.net" />
                <data android:host="www.e926.net" />
                <data android:host="e6ai.net" />
                <data android:host="www.e6ai.net" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
